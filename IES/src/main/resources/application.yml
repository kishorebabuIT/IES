server:
  port: 9090
  
spring:
  datasource: 
    username: system
    password: shareef
    url: jdbc:oracle:thin:@localhost:1521:orcl
    driver-class-name: oracle.jdbc.driver.OracleDriver
  jpa:
   properties:
     hibernate:
      show_sql: true
      format_sql: true
   hibernate:
     ddl-auto: update
  mvc:
     view:
        prefix: /views/
        suffix: .jsp